# # yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
# metadata:
#     template: azd-init@1.11.1
environment:
  name: conversation-knowledge-mining
  location: eastus
name: conversation-knowledge-mining
metadata:
  template: conversation-knowledge-mining@1.0

hooks:
  postprovision:
    windows:
      run: |
        Write-Host "Web app URL: "
        Write-Host "$env:WEB_APP_URL" -ForegroundColor Cyan
        Write-Host "🚀 If you want to deploy the AI Model, run the following command in the Bash terminal:"
        Write-Host "bash ./infra/scripts/deploy_ai_model.sh -ForegroundColor Cyan
        Write-Host "✅ If you want to use the Sample Data, run the following command in the Bash terminal to process it:"
        Write-Host "bash ./infra/scripts/process_sample_data.sh -ForegroundColor Cyan
      shell: pwsh
      continueOnError: false
      interactive: true
    posix:
      run: |
        echo "Web app URL: "
        echo $WEB_APP_URL
        echo "🚀 If you want to deploy the AI Model, run the following command in the Bash terminal:"
        echo "bash ./infra/scripts/deploy_ai_model.sh"
        echo "✅ If you want to use the Sample Data, run the following command in the Bash terminal to process it:"
        echo "bash ./infra/scripts/process_sample_data.sh"
      shell: sh
      continueOnError: false
      interactive: true
